{
  "builds": [
    {
      "src": "Angular/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "Angular/dist/Angular" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
