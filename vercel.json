{
  "version": 2,
  "builds": [
    {
      "src": "Angular/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/angular"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}